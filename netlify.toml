[build]
  command = "npm --workspace @sora/web run build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so vue-router history-mode routes (/login, /feed, …) resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
